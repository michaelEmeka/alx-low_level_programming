#include "main.h"
#include <stdio.h>
/**
 * print_array - this function prints n elements of
 * an array
 * @a: string parameter
 * @n: number of elements
 * Return: 0
 */
void print_array(int *a, int n)
{
	while (*a)
	{
		if (i > 0)
		{
			_putchar(',');
			_putchar(' ');
		}
		printf("%d", a[i]);
		a++;
	}
	_putchar('\n');
}
