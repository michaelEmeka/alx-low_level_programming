#include "main.h"

/**
 * main -tests function that prints if integer is positive or negative
 * positive_or_negative - checks if a number is
 * positive or negative
 * Return: 0
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
